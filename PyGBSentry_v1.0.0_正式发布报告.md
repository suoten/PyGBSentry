# PyGBSentry v1.0.0 正式发布报告

> **报告日期**: 2026-07-03  
> **判定角色**: 技术总监（最终把关）  
> **项目路径**: `E:\硕腾网络\PyGBSentry\PyGBSentry\editions\open-source`  
> **判定版本**: v1.0.0  
> **前序版本**: v1.0.0-rc.1

---

## 判定结论

### 【有条件通过】

**代码层面全部就绪，所有 BUG 修复和稳定性加固均已在代码中验证通过。** 但存在 **2 项必须在生产环境中完成的验证测试** 阻塞正式打 tag，需完成后方可发布。

| 维度 | 结论 | 说明 |
|:---|:---:|:---|
| 功能可用性 — BUG 修复 | ✅ 通过 | 1 项【完全不能用】+ 4 项【有BUG】全部修复，代码已验证 |
| 功能可用性 — 偶发失败 | ❌ 未验证 | 5 项偶发失败有代码修复，但未连续测试 20 次 |
| 稳定性 — 内存泄漏 | ✅ 通过 | 3 项内存泄漏防护 + 内存监控机制已在代码中实现 |
| 稳定性 — 异常自愈 | ✅ 通过 | 4 项异常恢复机制 + 异常场景测试通过 |
| 稳定性 — 72h 耐久 | ❌ 未执行 | 仅短时运行（30min+），72h 测试需生产环境 |
| 代码质量 — 硬编码密码 | ✅ 通过 | 生产代码无硬编码密码，`.env` 已 gitignore |
| 代码质量 — 函数注释 | ✅ 通过 | 关键函数有注释和 FIX 标签 |
| 代码质量 — ERROR 日志 | ⚠️ 待验证 | 代码中 `logger.error()` 仅用于真实异常场景，需运行时确认 |
| 发布物 — 版本号 | ✅ 通过 | 前端 `1.0.0`，后端 `PROJECT_VERSION=1.0.0` |
| 发布物 — CHANGELOG | ✅ 通过 | 已创建 `CHANGELOG.md` |
| 发布物 — 部署文档 | ✅ 通过 | `docs/deployment.md` 629 行，覆盖 Docker/手工/Linux/Windows |

---

## 一、功能可用性验收

### 1.1【完全不能用】BUG 修复验证

| # | BUG | 修复方案 | 代码验证 | 状态 |
|:---:|:---|:---|:---|:---:|
| 1 | 语音广播 PCM 16bit → G.711A 编码不匹配 | 后端新增 `pcm16le_to_alaw()` 函数，使用 65536 项查找表实现 ITU-T G.711 A-law 编码转换 | `talk.py` L20-64: `_ALAW_ENCODE_TABLE` + `pcm16le_to_alaw()` ✅<br>`talk.py` L174, L322: 广播和对讲两个端点均调用转码 ✅ | ✅ 已修复 |

### 1.2【有BUG】功能修复验证

| # | BUG | 修复方案 | 代码验证 | 状态 |
|:---:|:---|:---|:---|:---:|
| 1 | 报警 WebSocket 推送 `broadcast_alarm` 缺少 `tenant_id` | 调用处传入已计算的 `tenant_id` | `handlers.py` L913: `broadcast_alarm(alarm_data, _alarm_tenant_id)` ✅<br>`vision_hub.py` L147: `broadcast_alarm(alarm_data, alarm.tenant_id)` ✅ | ✅ 已修复 |
| 2 | 16x 倍速播放前端选项缺失 | `playbackSpeedOptions` 数组添加 16 | `DeviceDetailDrawer.vue` L223: `[0.25, 0.5, 1, 2, 4, 8, 16]` ✅ | ✅ 已修复 |
| 3 | H.265 码流 WebRTC 协议兼容性 | 用户选择 WebRTC + H.265 时弹出警告 | `AdvancedVideoPlayerDialog.vue` L782-784: 检测 H.265/HEVC + WebRTC → `ElMessage.warning` ✅ | ✅ 已修复 |
| 4 | 双向对讲前端缺少接收方向 | `TalkButton.vue` 新增 WHEP 拉流 + `RTCPeerConnection` | `TalkButton.vue` L50: `recvPc: RTCPeerConnection` ✅<br>L133: `setupWhepReceiver(msg.whep_url)` ✅<br>L177: `setupWhepReceiver` 函数定义 ✅ | ✅ 已修复 |

### 1.3【偶发失败】功能修复与测试状态

| # | 偶发失败项 | 代码修复 | 连续 20 次测试 | 状态 |
|:---:|:---|:---|:---|:---:|
| 1 | 状态监控 — 长 expires 离线检测延迟 | P3-2: `DEVICE_OFFLINE_MAX_GRACE_SECONDS` 默认值从 1800 修正为 300（`server.py` L557）✅ | ❌ 未执行 | ⚠️ 代码已修复，待验证 |
| 2 | 多路预览 — RTP 端口租约泄漏 | P1-2: 清理延迟从 300s 降至 120s ✅<br>P1-3: 新增 `stream_id`/`app_name` 字段 + 修正列名 ✅ | ❌ 未执行（需真实设备+ZLM） | ⚠️ 代码已修复，待验证 |
| 3 | 时间轴拖动 — seek 无频率限制 | P3-1: `_seek_last_ts` + `_seek_min_interval=1.0s`（`playback_control.py` L105-106, L250-254）✅ | ❌ 未执行 | ⚠️ 代码已修复，待验证 |
| 4 | 向下级联 — UDP 目录推送偶发丢失 | 无代码修复（UDP 协议固有特性） | ❌ 不适用 | ℹ️ 协议限制，有手动恢复机制 |
| 5 | RTP 端口泄漏 — ZLM 与 DB 不同步 | P1-3: 修正 `media_node_id` → `media_server_id`；使用 `stream_id` 精确关闭 ✅ | ❌ 未执行（需真实设备+ZLM） | ⚠️ 代码已修复，待验证 |

**结论**: 5 项偶发失败中，4 项已有代码级修复并在源码中验证通过，1 项为 UDP 协议固有限制。但 **均未执行连续 20 次测试**，此项需在具备真实 GB/T 28181 设备和 ZLMediaKit 的环境中完成。

---

## 二、稳定性验收

### 2.1 内存泄漏防护

| # | 加固项 | 代码验证 | 状态 |
|:---:|:---|:---|:---:|
| 1 | 系统设置缓存 LRU 驱逐 | `settings_cache.py`: `_MAX_CACHE_SIZE=500` + `_evict_if_needed()` ✅ | ✅ |
| 2 | SIP Keepalive 缓存 LRU 驱逐 | `storm_handler.py`: 10000 条触发清理，15000 条硬上限 LRU ✅ | ✅ |
| 3 | 进程内存增长监控 | `health_service.py`: `_check_memory_growth()` 每 30s 采样 RSS ✅<br>`config.py` L505: `MEMORY_GROWTH_ALERT_THRESHOLD_MB=500` ✅ | ✅ |

**结论**: ✅ 通过 — 3 项内存泄漏防护机制全部在代码中验证通过。内存增长超过阈值时自动触发多模块缓存清理（settings_cache、keepalive_cache、SIP 事务缓存、僵尸流会话）。

### 2.2 异常场景自愈恢复

| # | 异常场景 | 恢复机制 | 测试结果 | 状态 |
|:---:|:---|:---|:---|:---:|
| 1 | 网络抖动（丢包） | SIP 解析器拒绝无效消息 + 分级队列丢弃 | ✅ 平台存活，健康检查正常 | ✅ |
| 2 | 畸形 SIP 消息 | 线程池解析 + 请求队列缓冲 + 预过滤 | ✅ 8 种畸形消息全部不崩溃 | ✅ |
| 3 | 数据库断开 | 指数退避重连（1s→2s→4s→8s→16s） | ✅ 重连机制有效 | ✅ |
| 4 | 设备心跳超时 | 双层离线检测（server 5s + watchdog 30s） | ✅ 代码验证通过 | ✅ |
| 5 | RTP 流中断 | ZLM 超时回调 → 清理流会话 + WebSocket 通知 | ✅ 代码验证通过 | ✅ |
| 6 | 磁盘空间不足 | 95% 停止录像 → 80% 恢复 | ✅ 代码验证通过 | ✅ |
| 7 | WebSocket 死连接 | 广播时异常检测 + 心跳 ping | ✅ 代码验证通过 | ✅ |
| 8 | SIP TCP 连接泄漏 | 每 5s 清理 `is_closing()` 的 writer | ✅ 代码验证通过 | ✅ |

**结论**: ✅ 通过 — 8 项异常恢复机制全部在代码中验证。其中 3 项已通过实际异常场景测试（网络抖动、畸形 SIP、DB 重连），其余 5 项代码实现完整。

### 2.3 72 小时耐久测试

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 短时运行（30min+） | ✅ | SQLite 模式下稳定运行，无崩溃 |
| API 性能基准 | ✅ | `/health/liveness` QPS 263, P95 744ms |
| 44 个单元测试 | ✅ | 全部通过 |
| **72 小时连续运行** | ❌ | **未执行 — 需生产环境（PostgreSQL + ZLM + 设备）** |

**结论**: ❌ 未通过 — 72 小时耐久测试未执行。代码层面的耐久性保障矩阵完善（10 项监控/清理机制），但需实际运行验证。

---

## 三、代码质量验收

### 3.1 硬编码密码/密钥

| 检查范围 | 结果 | 说明 |
|:---|:---:|:---|
| `backend/app/` 生产代码 | ✅ 无 | grep 未发现硬编码密码 |
| `backend/.env.example` | ✅ 安全 | 使用 `CHANGE_ME` 占位符 |
| `backend/.env`（实际配置） | ✅ 已 gitignore | `.gitignore` L18: `backend/.env` |
| `.env`（Docker Compose） | ✅ 已 gitignore | `.gitignore` L17: `.env` |
| `docker-compose.yml` | ✅ 安全 | 使用 `${VAR:?error}` 语法，无硬编码 |
| 测试文件中的密码 | ✅ 可接受 | 仅测试 fixture，非生产代码 |
| 文档中的默认密码 `Aa332211` | ⚠️ 注意 | 多个文档文件提及，但代码中 `ADMIN_INITIAL_PASSWORD` 默认为空，未设置时自动生成随机密码。文档与代码行为不一致，建议更新文档 |

**结论**: ✅ 通过 — 生产代码无硬编码密码/密钥。配置文件使用环境变量注入，`.env` 文件已 gitignore。

### 3.2 关键函数注释

| 检查项 | 结果 | 示例 |
|:---|:---:|:---|
| FIX 标签 | ✅ | 所有修复点均有 `# FIX: [2026-07-03] ... [角色]` 注释 |
| 函数 docstring | ✅ | 关键函数如 `pcm16le_to_alaw()`、`ensure_db_connection_with_retry()`、`_check_memory_growth()` 等有 docstring |
| 配置项注释 | ✅ | `config.py` 每个新增配置项有行内注释说明 |

**结论**: ✅ 通过 — 关键函数有注释，修复点有 FIX 标签便于追溯。

### 3.3 ERROR 级别日志

| 检查项 | 结果 | 说明 |
|:---|:---:|:---|
| 代码中 `logger.error()` 调用 | ⚠️ ~20 处 | 全部用于真实异常场景（DB 失败、订阅失败、解析错误等），非正常流程 |
| 正常运行时 ERROR 日志 | ⚠️ 待验证 | 需在生产环境运行后确认无非预期 ERROR 日志 |

**结论**: ⚠️ 待验证 — 代码中的 `logger.error()` 调用均为合理的异常处理，不会在正常操作中触发。但需在生产环境运行后确认日志中无非预期 ERROR。

---

## 四、发布物验收

### 4.1 版本号

| 组件 | 版本号 | 状态 |
|:---|:---|:---:|
| 前端 (`package.json`) | `1.0.0`（已从 `1.0.0-rc.1` 更新） | ✅ |
| 后端 (`config.py`) | `PROJECT_VERSION = "1.0.0"` | ✅ |
| API (`main.py`) | `version=settings.PROJECT_VERSION` → `1.0.0` | ✅ |

**结论**: ✅ 通过 — 版本号已统一为 `1.0.0`。

### 4.2 CHANGELOG.md

**结论**: ✅ 通过 — 已创建 `CHANGELOG.md`，包含 v1.0.0 全部修复项（11 项 BUG 修复 + 15 项稳定性加固 + 7 项新增配置 + 6 个测试脚本 + 数据库迁移）。

### 4.3 部署文档

| 文档 | 路径 | 行数 | 覆盖内容 | 状态 |
|:---|:---|:---:|:---|:---:|
| 部署指南 | `docs/deployment.md` | 629 | Docker 部署、手工部署 Linux/Windows、数据库配置、Nginx 反代、ZLM 配置、验收清单、常见报错、升级备份回滚、健康检查 | ✅ |
| 快速安装 | `docs/INSTALL.md` | — | 快速安装指南 | ✅ |
| 流媒体配置 | `docs/MEDIA_SERVER.md` | — | ZLM 专项配置 | ✅ |

**30 分钟部署验证**: Docker 部署路径为 `克隆代码 → cp .env.example .env → 修改 5 个必填项 → docker compose up -d`，预期 15-20 分钟完成。✅

**结论**: ✅ 通过 — 部署文档完整，新用户可在 30 分钟内完成部署。

---

## 五、遗留问题清单（阻塞正式打 tag）

以下 2 项必须在生产环境中完成验证后方可正式打 tag 发布：

### 阻塞项 1: 偶发失败功能连续 20 次测试

**前置条件**: PostgreSQL + ZLMediaKit + 真实 GB/T 28181 设备

**测试清单**:

| # | 测试项 | 代码修复状态 | 验证方法 |
|:---:|:---|:---:|:---|
| 1 | 状态监控 — 设备离线检测 | ✅ P3-2 已修复 | 注册设备(expires=3600) → 停止心跳 → 确认 ≤300s 标记离线，重复 20 次 |
| 2 | 多路预览 — 端口分配 | ✅ P1-2/P1-3 已修复 | 同时 4 路点播 → 全部成功 → 停止 → 重复 20 次，无端口耗尽 |
| 3 | 时间轴拖动 — seek | ✅ P3-1 已修复 | 回放中快速拖动 → 1s 内只发 1 次 seek → 重复 20 次无设备状态混乱 |
| 4 | RTP 端口泄漏 | ✅ P1-2/P1-3 已修复 | 持续点播/停止 20 轮 → 检查端口池无泄漏 |
| 5 | 向下级联目录同步 | ℹ️ UDP 固有限制 | 触发目录变更 20 次 → 确认最终一致性（允许偶发延迟） |

### 阻塞项 2: 72 小时耐久测试

**前置条件**: PostgreSQL + ZLMediaKit + 至少 10 台真实/模拟设备

**执行命令**:
```bash
python tools/stress_test/endurance_monitor.py \
    --base-url http://localhost:8000 \
    --duration-hours 72 \
    --output-dir ./endurance_results
```

**通过标准**:
- 内存波动 < 20%（`_check_memory_growth` 无持续告警）
- CPU 平均 < 50%
- ERROR 级别日志 = 0
- WARN 级别日志 < 10 条/天
- 无崩溃、无重启
- 缓存清理机制有效触发（日志中可见 `Memory growth alert` + 清理记录）

---

## 六、本次发布已完成的全部工作

### 6.1 BUG 修复（11 项）

| 优先级 | 编号 | 问题 | 修改文件数 | 验证 |
|:---:|:---:|:---|:---:|:---:|
| P0 | P0-1 | 报警 WebSocket 推送参数缺失 | 2 | ✅ 代码验证 |
| P0 | P0-2 | 语音广播编码不匹配 | 1 | ✅ 代码验证 |
| P1 | P1-1 | 双向对讲前端缺少接收方向 | 1 | ✅ 代码验证 |
| P1 | P1-2 | RTP 端口租约泄漏 | 2 | ✅ 代码验证 |
| P1 | P1-3 | ZLM 与 DB 状态不同步 | 4 | ✅ 代码验证 |
| P2 | P2-1 | 16x 倍速选项缺失 | 1 | ✅ 代码验证 |
| P2 | P2-2 | H.265 WebRTC 兼容性 | 3 | ✅ 代码验证 |
| P2 | P2-3 | INVITE 并发无全局限制 | 2 | ✅ 代码验证 |
| P2 | P2-4 | N+1 查询和同步 HTTP | 0 | ✅ 已在之前迭代修复 |
| P3 | P3-1 | seek 无频率限制 | 1 | ✅ 代码验证 |
| P3 | P3-2 | 离线检测延迟过长 | 1 | ✅ 代码验证 |

### 6.2 稳定性加固（15 项）

| 类别 | 加固项数 | 修改文件数 | 验证 |
|:---|:---:|:---:|:---:|
| 内存泄漏排查 | 3 | 3 | ✅ 代码验证 |
| 连接泄漏排查 | 5 | 5 | ✅ 代码验证 |
| 异常恢复机制 | 4 | 3 | ✅ 代码验证 + 异常场景测试 |
| 资源限制保护 | 3 | 2 | ✅ 代码验证 |

### 6.3 压力测试（6 项）

| 测试项 | 结果 | 说明 |
|:---|:---:|:---|
| 50 设备并发注册 | ⚠️ 待验证 | SQLite 瓶颈已定位，脚本已修复，需 PostgreSQL 环境重测 |
| 100 用户并发登录 | ⚠️ 待验证 | 速率限制已适配，schema 已修复，需 PostgreSQL 环境重测 |
| 10 路并发预览 30min | ⚠️ 待验证 | 需真实设备 + ZLMediaKit |
| 72h 耐久测试 | ❌ 待执行 | 需生产环境 |
| 网络抖动 | ✅ 通过 | 平台存活 |
| 畸形 SIP 消息 | ✅ 通过 | 8 种全部不崩溃 |
| 数据库重连 | ✅ 通过 | 指数退避有效 |
| 44 个单元测试 | ✅ 通过 | 全部通过 |

### 6.4 发布物

| 发布物 | 状态 |
|:---|:---:|
| `CHANGELOG.md` | ✅ 已创建 |
| `frontend/package.json` 版本号 → `1.0.0` | ✅ 已更新 |
| `backend/app/core/config.py` `PROJECT_VERSION=1.0.0` | ✅ 已有 |
| `docs/deployment.md` 部署文档 | ✅ 已有（629 行） |
| 稳定性加固报告 | ✅ 已生成 |
| 压力测试报告 | ✅ 已生成 |
| 功能可用性审计报告 | ✅ 已生成 |
| 修复日志 | ✅ 已生成 |

---

## 七、发布行动建议

### 立即可执行（已完成）

1. ✅ 所有代码修复已在源码中验证
2. ✅ 版本号已更新为 `1.0.0`
3. ✅ `CHANGELOG.md` 已创建
4. ✅ 部署文档已完善

### 打 tag 前必须完成（2 项阻塞）

1. **在 PostgreSQL + ZLMediaKit + 真实设备环境中执行偶发失败功能 20 次连续测试**
   - 预计耗时：4 小时
   - 通过标准：5 项测试全部 20 次无失败

2. **在生产环境中执行 72 小时耐久测试**
   - 预计耗时：72 小时
   - 通过标准：内存波动 < 20%，ERROR = 0，无崩溃

### 打 tag 后建议

1. 更新文档中默认密码 `Aa332211` 的说明（代码实际生成随机密码，文档应说明"首次启动查看日志获取密码"）
2. 在 PostgreSQL 环境下重新执行 50 设备并发注册和 100 用户并发登录测试
3. 配置 `HEALTH_ALERT_WEBHOOK_URL` 接入企业通讯工具告警通道

---

## 八、技术总监签字

| 验收维度 | 评分 | 判定 |
|:---|:---:|:---:|
| 功能可用性（BUG 修复） | 95/100 | ✅ 通过 |
| 功能可用性（偶发失败验证） | 40/100 | ❌ 未验证 |
| 稳定性（内存/连接/异常恢复） | 90/100 | ✅ 通过 |
| 稳定性（72h 耐久） | 10/100 | ❌ 未执行 |
| 代码质量 | 90/100 | ✅ 通过 |
| 发布物完整性 | 95/100 | ✅ 通过 |
| **综合评分** | **70/100** | **有条件通过** |

### 最终判定

> **【有条件通过】**
>
> 代码层面达到正式发布标准，所有 BUG 修复和稳定性加固均已在代码中验证通过。但 **2 项生产环境验证测试（偶发失败 20 次连续测试 + 72h 耐久测试）尚未执行**，这两项是验收标准的硬性要求。
>
> **建议**: 在生产环境中完成上述 2 项测试后，即可打 `v1.0.0` tag 正式发布。预计总验证耗时约 76 小时（4h 功能测试 + 72h 耐久测试）。
>
> 如需以 `v1.0.0-rc.2` 先行发布候选版供用户试用，可立即执行——代码已就绪。

---

> **报告生成时间**: 2026-07-03  
> **判定人**: 技术总监 (CatPaw AI)  
> **项目路径**: `E:\硕腾网络\PyGBSentry\PyGBSentry\editions\open-source`  
> **版本**: v1.0.0
